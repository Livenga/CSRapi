# CSRapi

## 概要
C# で Windows CE(5.0) と通信を行わせるためのコード.  
ファイル検索等の機能は実装していない. 主にファイル転送を想定.


### 注意事項
``make``, ``gfind`` コマンドの使用を想定しているため, Gow のインストールが必要.  
また, 事前に任意のバージョンの .NET Framework の ``csc`` に対して, 事前に PATH を通す必要がある.


#### Version
